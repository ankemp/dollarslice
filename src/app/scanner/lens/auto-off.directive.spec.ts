import { AutoOffDirective } from './auto-off.directive';

describe('AutoOffDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoOffDirective();
    expect(directive).toBeTruthy();
  });
});
